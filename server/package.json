{
  "name": "server",
  "version": "1.0.0",
  "description": "Local static server for the Lexa PWA and published audio.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node ../scripts/build-data.js && node ../scripts/validate-data.js --strict-audio && node ../scripts/test-core.mjs && node ../scripts/test-lexeme.mjs && node ../scripts/test-extension.cjs && node ../scripts/security-check.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  }
}
